# =========================================================================
# Bloc à AJOUTER dans le Caddyfile de votre stack « radia-glam ».
# Ne remplace rien : il s'ajoute à côté de vos autres sites.
# Caddy provisionne automatiquement le certificat HTTPS (Let's Encrypt).
# =========================================================================

mnscapital.167.233.231.68.sslip.io {
    reverse_proxy mnscapital:3000
}
